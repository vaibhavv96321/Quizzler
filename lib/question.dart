class Question {
  String questionText = ' ';
  bool questionAnswer = true;

  Question(this.questionText, this.questionAnswer);
// {
  //   questionText = q;
  //   questionAnswer = a;
  // } this is the old school way we can also do it like above it is the same
}
